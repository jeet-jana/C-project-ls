#This project only work on linux and mac os 
#There are two commands "ls" and "ls -a" first one for show the files name and second one also shows files name but it also show hidden files like ".<folder name>" 
